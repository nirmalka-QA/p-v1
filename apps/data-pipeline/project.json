{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "data-pipeline",
  "projectType": "application",
  "sourceRoot": "apps/data-pipeline/src",
  "tags": ["type:app", "scope:remote"],
  "//": "Explicit targets (excluded from @nx/vite inference in nx.json): the @module-federation/vite config hangs Nx graph-node creation, so this remote runs Vite directly.",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "vite", "cwd": "apps/data-pipeline" },
      "continuous": true
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build", "cwd": "apps/data-pipeline" },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": { "command": "vite preview --port 4202 --strictPort", "cwd": "apps/data-pipeline" },
      "dependsOn": ["build"],
      "continuous": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p tsconfig.app.json", "cwd": "apps/data-pipeline" },
      "cache": true
    }
  }
}
