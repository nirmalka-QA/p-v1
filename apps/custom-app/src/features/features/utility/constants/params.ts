/**
 * URL search-param keys for the Features phase. The selected feature is the
 * single source of truth shared between the sidebar list and the main story
 * panel (mirrors Discovery / Planning).
 */
export const PARAM_FEATURE = 'feature'
