/**
 * URL search-param keys for the Test phase. The selected story is the single
 * source of truth shared between the sidebar list and the main test-case panel
 * (mirrors Features / Implementation).
 */
export const PARAM_STORY = 'story'
