/** Search-param keys driving the Implementation page's panel selection. */
export const PARAM_STORY = 'story'
export const PARAM_VIEW = 'view'

/** Values for the `view` search param (no story selected). */
export const VIEW_STACK = 'stack'
export const VIEW_REPO = 'repo'

/** Opens the setup wizard (`?setup=1`); set by the sidebar Settings action. */
export const PARAM_SETUP = 'setup'
/** Frontend/Backend workspace mode (`?mode=develop|code|preview`). */
export const PARAM_MODE = 'mode'
/** Section tab (`?tab=…`) for Database / Design. */
export const PARAM_TAB = 'tab'
/** Focus / full-screen mode (`?focus=1`) — hides the phase rail, sidebar, and assistant. */
export const PARAM_FOCUS = 'focus'
