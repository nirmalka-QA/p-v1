/**
 * URL search-param keys used to share Planning view state between the sidebar
 * and the main panel without coupling their component trees (the URL is the
 * single source of truth for the selected feature).
 */
export const PARAM_FEATURE = 'feature'
