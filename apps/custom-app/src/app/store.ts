// The store is owned by @wispr/store (created by the host, or by this app's
// standalone bootstrap). Re-export the dispatch type so existing feature code
// keeps importing it from here unchanged.
export type { AppDispatch } from '@wispr/store'
